version: '3.8'
services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: leetcode-search-engine
    ports:
      - "8000:8000"
    restart: unless-stopped
    volumes:
      - ./data:/app/data
